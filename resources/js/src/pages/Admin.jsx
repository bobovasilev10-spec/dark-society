import {useState} from 'react'; import products from '../products.json';
export default function Admin(){const [value,setValue]=useState(JSON.stringify(products,null,2));const addDemo=()=>{try{const p=JSON.parse(value);p.push({id:'new-product',name:'New Product',price:49,category:'t-shirts',color:'Black',badge:'NEW',images:['images/products/product-1.png','images/hero/hero-urban.png'],sizes:['S','M','L','XL'],description:'Edit this product.',materials:'Cotton.',stock:10});setValue(JSON.stringify(p,null,2))}catch{alert('Invalid JSON')}};return <section className="page-pad section"><div className="container"><div className="kicker">No backend v1</div><h1>Product JSON Editor</h1><p>Edit and copy the result into <strong>src/products.json</strong>.</p><div className="admin-actions"><button className="btn" onClick={addDemo}>Add demo product</button><button className="btn ghost" onClick={()=>navigator.clipboard.writeText(value)}>Copy JSON</button></div><textarea className="json-output" value={value} onChange={e=>setValue(e.target.value)}/></div></section>}
