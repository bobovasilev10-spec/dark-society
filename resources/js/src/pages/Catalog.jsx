import {useEffect,useState} from 'react';import ProductCard from '../components/ProductCard';import fallback from '../products.json';import {api} from '../api';
export default function Catalog(){const [products,setProducts]=useState([]);const [q,setQ]=useState('');useEffect(()=>{api.search(q).then(r=>setProducts(r.products||[])).catch(()=>setProducts(fallback))},[q]);const list=products.length?products:fallback;return <section className="page-pad section"><div className="container"><div className="section-head"><div><div className="kicker">All products</div><h1>Catalog</h1></div><input className="shop-input" value={q} onChange={e=>setQ(e.target.value)} placeholder="Search products"/></div><div className="products-grid">{list.map(p=><ProductCard key={p.id} product={p}/>)}</div></div></section>}
