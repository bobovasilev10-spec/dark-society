import {useState} from 'react';import {api} from '../api';export default function Contact(){const [f,setF]=useState({name:'',email:'',phone:'',message:''});const [status,setStatus]=useState('');const send=async e=>{e.preventDefault();try{await api.contact(f);setStatus('Message sent.')}catch(e){setStatus(e.message)}};return <section className="page-pad section"><div className="container narrow"><h1>Contact</h1><form className="shop-form" onSubmit={send}>{Object.keys(f).map(k=><label key={k}>{k}<input value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/></label>)}<button className="btn">SEND</button>{status&&<p>{status}</p>}</form></div></section>}
