import Home from "../../components/Home/Home";

const HomePage = ({ newProducts, subcriptions }) => {

    return (
        <Home newProducts={newProducts} subcriptions={subcriptions} />
    );
};

export default HomePage;

